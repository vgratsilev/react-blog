import { Counter } from './Counter';
import '../index.scss';

const App = () => {
    return (
        <div className={'app'}>
            App text
            <Counter />
        </div>
    );
};

export default App;
